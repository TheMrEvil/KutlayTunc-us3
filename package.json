{
  "name": "us3-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "us3 — composable, R3F-native game-AI + animation runtime. A behaviour tree + perception + navmesh path-following + EQS brain, and a blend-space + state-machine animation graph — headless, with one-line R3F hooks that compose with the pmndrs stack (triplex, ecctrl, recast-navigation).",
  "license": "MIT",
  "author": "KutlayTunc",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KutlayTunc/us3.git"
  },
  "homepage": "https://github.com/KutlayTunc/us3",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf dist node_modules/.vite tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=20"
  }
}
